# ASCII Image Split Site

This is a demo HTML/CSS project that showcases an ASCII-art "image" that splits apart when you hover over it.

## Features

- Animated rhombus background
- Character-based image slicing
- Hover-triggered animation
- Organized folder structure

## How to Use

Open `index.html` in a browser, or host the site using any static server (e.g., GitHub Pages).
